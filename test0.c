#include  "malloc.h"

int main()
{
  int i;
  char *addr;

  i = 0;
  while (i < 1024)
  {
    i++;
  }
  // ft_putnbr_base((long long)malloc(1), 16);
  // printf("%lu", (unsigned long)malloc(1));
  return (0);
}
